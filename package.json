{
  "name": "locast2m3u",
  "version": "1.0.0",
  "description": "generate a m3u file from Locast Stream",
  "main": "ripper.js",
  "dependencies": {
    "form-data": "^2.3.2",
    "node-fetch": "^2.2.0"
  },
  "devDependencies": {
    "dotenv": "^6.0.0"
  },
  "scripts": {
    "start": "node -r dotenv/config -e 'require(\"./ripper.js\").init()'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Digioi",
  "license": "ISC"
}
